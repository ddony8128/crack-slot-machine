// Test stub for the `server-only` package, which otherwise throws when imported
// outside a React Server Component bundle. Aliased in vitest.config.ts so
// server-only helpers can be unit-tested under jsdom.
export {};
